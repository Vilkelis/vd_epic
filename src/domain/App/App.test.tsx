import React from 'react'
import { render } from '@testing-library/react'

import App from './App'

test('renders container', () => {
  // const { getByText } = render(<App />)
  // const linkElement = getByText(/Tiger of Sweden/i)
  // expect(linkElement).toBeInTheDocument()
  return true
})
